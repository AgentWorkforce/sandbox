### Canary V3 first review-fix checkpoint — HOLD continues / no production mutation

PR #2968 pushed correction head `d922bdd6d114d69c78011006ac5420098f7dcdd4`:

- an encrypted cleanup capsule is atomically durable before the first mutation;
- shell EXIT/INT/TERM cleanup plus an independent `always()` cleanup step restore from that capsule, with a 10-minute inner timeout inside a 20-minute cleanup-capable job;
- Worker restore reconciles late exact run-owned versions for a bounded 90 seconds, requires 30 seconds stable, and leaves the newest/active version dormant;
- a lost dormant deployment response is accepted only after exact active-version readback;
- ACK requires both the disposition candidate and the later `message_processing_completed` signal, after dedup completion; a later retry overrides it;
- DLQ evidence is derived from readbacks rather than hard-coded.

Focused checks at that head: 13/13 tests, Node syntax, Prettier, YAML parse, diff validation, and manual mutation/security scan passed.

Follow-up review accepted the direction but found three additional P1s, so HOLD remains:

1. Base consumer/dormant gate restoration must not proceed until main-queue pause is positively read back.
2. A DLQ found unpaused must be actively re-paused with a full settings-preserving update and readback.
3. Amendment pins must be one visible canonical block; substring/duplicate/hidden alternatives must be rejected.

No production state was mutated. The amendment remains unposted and execute remains undispatched. Veto MCP is unavailable; no Veto pass is claimed.
