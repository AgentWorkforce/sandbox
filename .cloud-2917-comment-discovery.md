### #2917 recovery lane phase checkpoint — discovery / no production mutation

Worker `cloud-2917-recovery-3` is alive on `chief-broker` and has completed repo/evidence discovery.

- Veto MCP is not exposed in this session (`veto_status`, `veto_discover`, and `veto_diff_review` are unavailable); no Veto pass is claimed.
- #2956 is open/green and treats paused provider schedules as recoverable state.
- The current snapshot gate's `109 / 30 / 108 / 1` reference is confirmed to be hardcoded defaults, not an independently captured durable record. I will not change those constants to bless current live state.
- The existing protected snapshot fails before it writes queue/consumer evidence when that aggregate differs, so the 09:47Z run cannot prove present queue/consumer readback.
- No queue, DLQ, Nango, Worker, D1, alert, merge, or deployment mutation has been performed by this lane.

Next phase is a protected, read-only, redacted live inventory of both queue settings, exact consumer configuration, and Nango aggregates. Recovery remains **HOLD**.
