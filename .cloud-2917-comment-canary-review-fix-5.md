### #2917 Queue canary V3 review-fix checkpoint — HOLD

- PR: #2968; exact pushed head: `4973a04d78703ca5c67173d0ac08a2f1206444d0`.
- Fixed rendered-entity ambiguity: amendment bodies now reject numeric, hex, and named character-reference syntax before normalization, including the reviewed `W&#x6f;rker` conflicting-pin bypass.
- Fixed inverse deployment race: Worker ownership and both Queue holds are now obtained as one joint proof. The whole proof must complete within five seconds or fail before POST; only a fresh proof showing the previous/candidate Worker and both Queue holds may proceed immediately to deployment.
- Focused verification: 19/19 combined controller + snapshot tests green; tests cover the exact entity bypass, a 5,001ms stale proof, and joint ownership/holds → deployment POST → readback. Syntax, Prettier, diff, mutation allowlist, redaction, and manual security review are green.
- Both fresh review threads were answered and resolved. Full exact-head CI and exact-head review are pending, so this remains HOLD.
- No production Queue, Worker, D1, DLQ, or Nango mutation occurred. The amendment is not posted and execute is not dispatched.
- Veto MCP is unavailable in this lane; no Veto pass is claimed.
