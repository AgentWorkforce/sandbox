### Recovery lane checkpoint — full encrypted snapshot dispatched (2026-08-08T23:49:17Z)

Protected run [`31284831128`](https://github.com/AgentWorkforce/cloud/actions/runs/31284831128) was dispatched from exact `main` SHA `e3a4682826fa12a56ecb3fd08b3668ac75fb2416` in full encrypted-evidence mode.

It is pinned to the immediately preceding protected inventory: 109 schedules / 28 scheduled connections / 30 registered connections / 91 paused / 18 active, plus exact keyed membership, registered-membership, and control-state HMACs. Any count, membership, or control-state drift fails closed before an evidence file is written. Exact bodies exist only in AES-256-GCM envelopes; the workflow self-verifies decryption before upload.

Initial state is `waiting`. This workflow is read-only against Queue, Worker, and Nango resources. Terminal result and artifact validation are pending. Veto MCP is unavailable, so no Veto pass is claimed.
