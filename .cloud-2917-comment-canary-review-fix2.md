### Canary V3 containment and ownership correction checkpoint — no production mutation

PR #2968 is pushed at exact head `1f9364946b4616eeb93630a8c3ab6fa4960e32e8`. All known review threads are answered/resolved; unresolved count is currently 0.

Corrections since the prior checkpoint:

- Cleanup requires a fresh main-paused GET before consumer restore, a fresh base-consumer GET afterward, and another main-paused GET before Worker dormancy. Missing proof leaves the constrained consumer and/or cap armed and returns RED.
- Cleanup actively repairs a DLQ pause violation with a full settings-preserving PUT and fresh paused GET. Evidence separately records “violation observed” and final pause readback, so repair cannot rewrite history.
- The amendment must start with one exact visible canonical block; each heading/pin label occurs exactly once, and HTML/comments/details or fenced content are rejected.
- The unique canary run ID is now atomically claimed before Worker activation by a D1 `INSERT ... RETURNING` with cap 10/count 0 and no conflict handler. Any collision fails closed. The row is preserved as evidence and is never deleted.

Focused result: 15/15 tests pass; Node syntax, Prettier, YAML parse, diff validation, and manual destructive-path/security review pass. Full CI and a fresh exact-head review are pending; no final pass is claimed.

No production state was mutated. The amendment remains unposted and execute remains undispatched. Veto MCP is unavailable; no Veto pass is claimed.
