### #2917 RED/HOLD — telemetry response-shape fix PR

PR #2970 is open at pushed head `cb35ece68` after read-only diagnostic run `31292196784` failed before evidence creation with `telemetry-query-shape-invalid`.

Cloudflare accepted the query, but its documented optional aggregate `count` was absent and our parser incorrectly required it. The fix:

- accepts omitted count only below the requested 2,000-row boundary;
- still rejects explicit total greater than returned rows;
- rejects invalid totals, totals smaller than returned rows, and any 2,000-row boundary response as ambiguous truncation;
- changes no endpoint, permission, evidence, encryption, or production control path.

Local gates: 26/26 focused tests, syntax, Prettier, and diff check green. Veto MCP is unavailable; no Veto pass is claimed. CI/review pending.

**Production remains contained. No diagnostic retry, canary retry, drain, replay, DLQ disposition, purge, or Nango action has occurred.**
