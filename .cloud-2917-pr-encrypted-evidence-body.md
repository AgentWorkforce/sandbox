## Summary

- encrypt exact #2917 production snapshot and Nango manifest in memory with AES-256-GCM before any artifact write
- require exact schedule/registered counts plus immutable-membership, registered-membership, and PAUSED/frequency HMAC pins from a preceding stable inventory before full mode can perform network reads
- retain raw SHA-256 digests and provider/connection/request-body mappings only inside encrypted evidence; publish only keyed HMACs/pseudonyms and encryption metadata
- add a PAUSED/frequency control-state HMAC so equal aggregate counts cannot mask tuple-level state drift

## Safety

- inventory-only behavior remains GET-only and stores no exact identifiers
- full mode fails before network access if any pin or the canonical 32-byte evidence key is absent
- wrong pins fail before any evidence file is written
- AES-GCM envelopes use independent 96-bit random IVs, authenticated AAD, auth tags, key fingerprint, and a key-bound plaintext HMAC; each envelope is self-decrypted in memory before write
- plaintext `production-snapshot.json` and `nango-manifest.json` are no longer written
- no Queue, DLQ, Worker, Nango, D1, alert, or secret mutation is included in this PR

## Verification

- `node --test tests/incident-2917-snapshot.test.mjs`
- `npx prettier --check .github/workflows/incident-2917-production-snapshot.yml scripts/incident-2917-snapshot.mjs tests/incident-2917-snapshot.test.mjs`
- `node --check scripts/incident-2917-snapshot.mjs`
- `git diff --check`
- fixtures decrypt exact evidence, reject ciphertext tampering, prove plaintext files are absent, prove missing/wrong pins fail closed, and retain GET-only/redaction/stability coverage

Veto MCP is unavailable in this session, so no Veto pass is claimed. `veto_diff_review` must not be represented as run.

Closes no issue; this is a bounded prerequisite for #2917's written canary amendment.
