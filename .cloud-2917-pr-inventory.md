## Summary

- add an explicit inventory-only mode to the #2917 production snapshot
- capture redacted queue settings and consumer configuration even when the hardcoded Nango reference does not match live state
- keep connection identifiers and immutable request bodies out of inventory artifacts
- label the Nango aggregate as live state, not an independently reconciled durable baseline

## Safety

- inventory mode performs only GET requests
- no queue, DLQ, Nango, Worker, D1, or alert mutation
- strict snapshot behavior remains fail-closed when inventory mode is disabled

## Validation

- `node --test tests/incident-2917-snapshot.test.mjs`
- targeted Prettier check
- `node --check scripts/incident-2917-snapshot.mjs`
- `git diff --check`
- Veto MCP unavailable in this session; no Veto pass claimed
