## Summary

- normalize Cloudflare Worker schedule GET responses from the documented `{ schedules: [...] }` result and the legacy direct-array shape
- keep unsupported response shapes fail-closed as UNKNOWN
- publish redacted sorted cron expressions, count, and pinned digest in the protected inventory checkpoint
- update the fixture to exercise both documented and legacy shapes

## Why

Run `31280246824` resolved the Queue consumers and Worker but reported the queue-health cron RED because the snapshot treated `result` as an array. Cloudflare's official ScheduleGetResponse is an object containing `schedules`; therefore that RED was an instrumentation false negative and cannot authorize a repair.

## Verification

- `node --test tests/incident-2917-snapshot.test.mjs`
- `npx prettier --check scripts/incident-2917-snapshot.mjs tests/incident-2917-snapshot.test.mjs .github/workflows/incident-2917-production-snapshot.yml`
- `node --check scripts/incident-2917-snapshot.mjs`
- `git diff --check`

No production workflow was dispatched from this branch. Veto MCP is unavailable, so no Veto pass is claimed; full CI/review/manual security evidence is required before merge.

Relates to #2917.
