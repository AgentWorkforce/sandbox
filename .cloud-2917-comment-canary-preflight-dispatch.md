### #2917 Queue canary V3 — read-only preflight dispatched

Protected `mode=preflight` run `31290835058` has been dispatched from `main`, pinned to the exact Worker code-etag HMAC from post-deploy readback.

This preflight is limited to live control-state, queue/consumer, Worker identity, RelayAuth, D1, and bounded tail-observability checks plus encrypted evidence. It does not arm admission or mutate Queue, Worker, DLQ, or Nango state.

Terminal result and artifact are pending. **The amendment remains unposted; execute remains undispatched.** Any RED or ambiguous/lost-observability result stops here.
