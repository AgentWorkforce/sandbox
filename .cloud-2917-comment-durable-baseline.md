### #2917 phase checkpoint — independent durable baseline found / no production mutation

Read-only local custody discovery found a pre-#2960 record at `cloud/.agentworkforce/relay/custody/nango-containment-20260804T031922Z.json` (mode `0600`; record SHA-256 `03e90048ed73e44e45041fb7eb3d38121450e9b1f9f4a934326a6e880bfc6dbe`). It was captured at `2026-08-04T03:19:22.054Z` and contains no tuple identifiers.

The record independently pins old manifest SHA-256 `42e8d516ddda96cda8136169dd191c2776e09a508d1769515cd6eabf689fe2fc` and records:

- 30 connections; 30 successful status reads; zero failed reads or connection duplicates
- 109 manifest rows and 109 live rows
- zero missing, duplicate, or extra rows
- `PAUSED=108`, `SUCCESS=1`, and zero status mismatches
- `freshGreen=true`, `mutationsPerformed=0`, `secretOrTupleIdentifiersEmitted=false`

This changes the reconciliation finding: `109 / 30 / 108 / 1` is not only a hardcoded default; it has an independently persisted, hash-pinned pre-script custody record. The historical aggregate is therefore a legitimate baseline. The old plaintext request bodies are still unavailable, so the record does **not** by itself reconstruct the exact current tuple delta or authorize changing Nango state.

No production access or mutation occurred in this phase. Queue canary V3 and Nango waves remain **HOLD** pending the protected live inventory and exact evidence reconstruction.
