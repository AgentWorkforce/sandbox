### #2917 Step 0 complete — no partial Queue/Worker amendment; Nango hold remains

Protected inventory run `31281089022` completed successfully from exact `main` SHA `3678de57e7ff3d638e6f88b936b3e80208ac8c0e`. GitHub pins the artifact at SHA-256 `fc1b492728efe012b8ccde82aaa79dcecd34b1e348d4e75cf9caaf049a4a459a`; downloaded internal `SHA256SUMS` validates, and `public-summary.json` is SHA-256 `a2d7ceeed3bef2063381698b050f358c843ff2a4ca5b54aeec6aee10f65d3e31`.

**Stable production readback (2026-08-08T22:11:22Z):**

- Main Queue: 15,256 messages / 257,627,857 bytes; `delivery_paused=true`.
- DLQ: 373 messages / 8,343,627 bytes; `delivery_paused=true`.
- Endpoint, Queue detail, and per-consumer GET agree: exactly one fully resolved worker consumer per queue; both use the same production Worker.
- Main config: DLQ `webhook-events-dlq`, batch 50, max retries 5, wait 5,000 ms, retry delay 0.
- DLQ config: no downstream DLQ, batch 50, max retries 1, wait 5,000 ms, retry delay 0.
- Admission gate is dormant: available=true, enabled=false, no run ID.
- Queue-health cron is GREEN: exactly one schedule, `*/5 * * * *`, digest `f3f2e2a08d10d009741be24858b1aea64033b83b0f4641a2a68f8ae16e8eb85f`.
- RelayAuth health/JWKS are 200/200 with one key. D1 grew 12,288 bytes over 10 seconds; rollback D1 count remains 36. This sample is recorded, not labelled normal or abnormal without a bounded baseline.
- Nango remains 109 schedules / 28 connections / 91 PAUSED / 18 active, with immutable membership digest unchanged at `1e365a14d42834b012e197b6e09dda609ea5ecda07999bf4bb7accbc092c43f6`.

**Step-0 conclusion:** production was not left mid-amendment on the Queue/consumer/Worker/admission/cron surface, and no Queue restoration is required. The earlier zero-consumer and cron-RED findings were both instrumentation false negatives and have been corrected durably.

The Nango delta is independent and unchanged from the 09:47Z hold. The historical aggregate baseline is trustworthy, but the old exact request bodies are not available locally or in surviving GitHub artifacts; no membership/state reconciliation will be guessed. Nango and both Queue delivery holds remain in place pending exact durable reconciliation and the written `#2917-QUEUE-CANARY-V3` amendment.

No production mutation was performed in Step 0. No event was purged, deleted, replayed, or dispositioned.
