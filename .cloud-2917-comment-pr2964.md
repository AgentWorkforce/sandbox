### #2917 phase checkpoint — lossless consumer identity follow-up open / no mutation

PR #2964 is open at pushed head `0fa53270f5da676bd7aa8a4eea4471fc9c28e91c`.

This read-only amendment uses Cloudflare's documented GET-by-consumer-resource endpoint for each already-listed consumer. It:

- never creates, updates, deletes, pauses, resumes, purges, or replays anything;
- keeps consumer resource identifiers out of inventory artifacts, console output, workflow comments, and error labels;
- reports requested/resolved/failed detail counts per queue;
- accepts topology only when main and DLQ each resolve one worker consumer and both identify the same Worker;
- leaves admission and cron UNKNOWN on any missing/failed/mismatched observation.

Targeted Node tests, Prettier, Node syntax, and diff checks pass. Coverage includes the lossy list response observed live, successful detail resolution, zero consumers, detail-fetch failure, identifier redaction, and GET-only execution. The branch is pushed; no production workflow was dispatched.

Veto MCP remains unavailable; no Veto pass is claimed. Full applicable CI, automated review, unresolved-thread check, and manual security/secrets review are pending before any merge.
