## Summary

- let #2917 inventory-only runs durably record zero or ambiguous main Queue consumer topology instead of aborting before evidence
- mark Worker admission/cron readback unavailable and topology RED when there is not exactly one consumer
- preserve strict fail-closed behavior for non-inventory recovery snapshots

## Incident evidence

Protected run 31277551851 reported `Expected one main Queue Worker consumer, found 0`. That live finding is inconsistent with the retained one-consumer containment baseline and must be captured before any repair.

## Safety

- inventory mode remains GET-only and writes only the redacted public summary
- no queue, DLQ, Nango, Worker, D1, or alert mutation
- strict recovery mode still refuses zero/multiple consumers or a missing queue-health cron

## Validation

- `node --test tests/incident-2917-snapshot.test.mjs`
- targeted Prettier check
- Node syntax check
- `git diff --check`
- zero-consumer regression proves successful redacted capture with topology/cron RED and admission unavailable
- Veto MCP unavailable in this session; no Veto pass claimed
