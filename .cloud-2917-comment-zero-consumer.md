### #2917 Step 0 checkpoint — RED: main Queue reports zero consumers / no mutation

Protected read-only inventory run [31277551851](https://github.com/AgentWorkforce/cloud/actions/runs/31277551851) reached live Cloudflare/Nango APIs and failed before artifact creation on this exact topology readback:

> `Expected one main Queue Worker consumer, found 0`

This is a separate and more immediate finding than the Nango status drift. `webhook-events` currently reports **zero consumers**, inconsistent with the independently retained `2026-08-04T03:49:58Z` containment record (`consumerCount=1`) and with the prior canary's restored base consumer. A paused queue with no consumer cannot drain when delivery is resumed; this can independently explain the dead provider feed.

No queue, DLQ, Nango, Worker, D1, or alert mutation occurred in this run. Both queues and all Nango waves remain **HOLD**. The next phase will make inventory tolerate zero/multiple consumers only long enough to durably capture the redacted queue/DLQ/settings evidence, then reconstruct and restore the exact previous consumer configuration from durable evidence before any V3 canary. Nothing will be guessed.
