### #2917 phase checkpoint — unavailable cron is now UNKNOWN / no mutation

Review of PR #2962 correctly found that a zero-consumer inventory cannot inspect the consumer Worker's schedules, so reporting the queue-health cron as RED would overstate the evidence. Head `007ea454b` now records the cron readback as unavailable/`UNKNOWN`; strict recovery mode still requires a proven GREEN cron. The P2 thread is fixed and resolved, and all targeted checks re-pass.

Full applicable gates restarted. No protected branch run or production mutation occurred; recovery remains **HOLD**.
