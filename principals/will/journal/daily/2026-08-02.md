---
date: 2026-08-02
repos: [chief, relay]
tags: [fleet-outage, restart, watchdog, voice, codex, review-queue]
---
# 2026-08-02

## Shipped
- **Fleet recovered from a ~42h full outage.** All 15 residents were
  BROKER_DOWN since Fri 2026-07-31 ~13:03 ET. Root cause (corrected by
  relay's code-verified analysis, filed as **relay#1416**): a machine-wide
  DNS outage made Relaycast registration at `cast.agentrelay.com` fail
  with a transport error; `connect_relay` retries only elapsed timeouts
  and treats returned transport errors as definite failures, so `init`
  propagated fatally and every node exited 1. The PostHog
  `ENOTFOUND i.agentrelay.com` stack that dominates the logs is a
  simultaneous secondary symptom, not the killer. Launchd plists carry no
  KeepAlive, so nothing came back. Chief's node returned 11:11Z (launchd
  kickstart), the other 14 kickstarted 11:13Z. Verified: 15/15 brokers
  online, full roster re-registered on the org workspace (no re-homing —
  the 07-30 store/state alignment held), inbound-delivery proofs
  collected via #general ACKs.
- **relay#1416 filed** (relay lead, DONE with evidence): classify/retry
  transient DNS/transport errors, keep the telemetry-never-fatal
  invariant, node self-recovery without manual kickstart, degraded
  readiness, regression tests. Rides the patch-release conversation.
- **voice seat swapped to codex spark** per Will: `codex --model
  gpt-5.3-codex-spark` in both `teams.json` and `teams.will.json`
  (rename-staged `voice-will` entry), applied live via per-seat
  `node agent release` + `spawn` — no chief-node bounce needed.
- **RQ-2 unstuck:** exact reconnect command sequence sent to Will via
  voice (login → `workspace join rw_7ccfea89` → `integration connect
  github` → verify). RQ-12 queued: cso + cpo lack git remotes (verified
  2/107), Will owns the private-repo/visibility call.

## Learned
- The watchdog's BROKER_DOWN×15 was REAL this time (contrast 07-30's
  stale one). Detection worked; **paging didn't while chief was down** —
  the 06:13Z page failed (`ok:false`), because the only page target was
  itself a casualty. Kickstart stayed manual for ~42h.
- launchd node jobs die permanently on a single crash: `RunAtLoad` only,
  no `KeepAlive`. Fix queued as a chief-repo PR (KeepAlive
  SuccessfulExit=false + throttle in install.mjs).

## In flight
- ACK sweep: proven so far — relay, burn, relaycron-cloud, agentrelay-com,
  cmo, cso, head-of-ecosystem, voice(pre-swap). Awaiting: cloud, cpo,
  scout, mobile, chief-app, hoopsheet, ladd, relayfile, heads/others.
- cmo: `/skill` page still serving `node up|status` (re-verified live
  today); their PR #38 ruling due to chief within the hour.
- head-of-ecosystem advisory adopted fleet-wide: pre-Fri-13:03 inbox items
  are stale until re-verified; in-flight reviews presumed dead.
- RQ-13 queued: agentrelay.com production environment gate — Will
  confirms khaliqgant as required reviewer (willwashburn is structurally
  void: every agent authenticates as it) + a human runs the ~90-sec
  environment creation before the workflow PR merges. cmo's finding that
  GitHub silently drops author==requestee review requests makes
  Relay-DM review sourcing the org mechanism, not a workaround.
- Khaliq active in the chief repo today (4057fb1 removed
  docs/organizations-and-surfaces.md — his own commit, his call). His
  chief-khaliq seat has still not joined the org workspace; the rename
  flip stays staged.
- relay#1397: resolved to draft #1405 (completed Friday, pre-outage);
  routed GO to relay — reconcile onto main, un-draft, two fresh reviewers
  at the new head. head-of-ecosystem's duplicate assignment was withdrawn
  cleanly.
- **Class C credential leak** (cpo escalation, eb4a0b2): installed
  agent-relay 11.3.1 still prints an unmasked workspace key via
  `@relayflows/core` runner.js:2857 on the first-run path — a dependency
  reproduces the leak inside the fixed release. Routed to relay; then
  re-scoped by head-of-ecosystem (relayed to relay mid-flight):
  `scrubSecrets` exists but SECRET_PATTERNS misses all `_live_`-format
  keys, and the leak spans every checked checkout (five versions, all
  leak) — fix is pattern extension + sanitizer at the workflow runner's
  auto-created-key guard; pinning is reproducibility only. Reachability
  corrected twice more (cpo/cmo): fires on workflow execution only
  (local-workflow/cloud-worker, three conjunctive conditions), NOT any
  node-lifecycle first-run path; and "11.3.1 closes the exposure" is
  false unscoped — always name the command family. Shipped as relayflows#25/#26
  (issues) + relayflows#27 (fix PR at 02fcf8fff, 817 tests green,
  reviewers cso + relayfile) + relay#1417 (exact-pin, deliberately
  draft until the human-approved republish). Class A/B "complete"
  language is superseded. Method rules promoted to learnings (installed
  tree not tarball; symbol-name absence is not absence).
- cmo lifted its agentrelay.com#38 hold (ruling on record in cmo):
  amendment assigned to agentrelay-com (exit criterion must cite
  "11.3.1 or later", not sha 3c5046a6); merge stays with chief pending
  two recorded reviews at the amended head; production deploy path has
  no GitHub environment/approver — cheap fix flagged.
