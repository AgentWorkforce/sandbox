---
date: 2026-07-31
repos: [chief]
tags: [chief, naming, multi-principal]
---
# 2026-07-31

## In flight
- **Chief rename staged (Will's call): `chief-will` / `chief-khaliq`,
  convention `<role>-<principal>` for principal-scoped seats,
  role-only names for org/department seats.** Staged inert in the
  committed variants (teams.will.json, chief.config.will.json,
  teams.khaliq.json, chief.config.khaliq.json, principals/README,
  onboarding runbook §5); the live seats keep `chief`/`voice` until the
  flip. Khaliq's side adopts on his next config regeneration — his seat
  has not joined the org workspace yet, so his rename is free.
- **Flip runbook — execute when `chief-khaliq` joins the org workspace
  (or at a coordinated boundary), not before:**
  1. `cp chief.config.will.json chief.config.json && cp teams.will.json
     teams.json`
  2. Flip live-read name references: `principals/will/docs/voice.md` and
     `.claude/skills/voice-talk/SKILL.md` (`chief` → `chief-will`);
     `tools/orgchart/org.json` (`chief` → `chief-will`, `voice` →
     `voice-will`, including `reportsTo`); watchdog DM target
     (`WATCHDOG_DM_TARGET=chief-will` in its plist, or flip the default
     in `tools/watchdog/fleet-watchdog.mjs:61`).
  3. `agent-relay down`, then `launchctl kickstart -k
     gui/501/com.agentworkforce.chief.node` — kills the resident by
     design; the successor resumes from files (restart-handoff
     discipline).
  4. Release the stale registry rows `chief` and `voice`; verify the
     roster via list_agents.
  5. Broadcast in #general: the chiefs are `chief-will` and
     `chief-khaliq`, the voice is `voice-will`; every seat updates its
     durable references ("DM chief" → "DM chief-will").
  6. Inbound-delivery proofs for both chiefs (outbound-evidence
     standard); kickstart `com.agentworkforce.orgchart`.
