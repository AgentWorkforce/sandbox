---
month: 2026-04
backfilled: 2026-07-29
repos: [relay, relaycast, relayfile, relayauth, relayfile-adapters, workforce, relayflows, pear, cloud, relaycron, burn, skills]
---
# 2026-04 — packaging fixes and the CLI arrives

## core-messaging (relay)
- v4.0.0 defaults to production cloud endpoints with a **unified workspace ID**
  across relay services.
- **v5.0.0 + v6.0.0 end the install-failure era**: Python wheels with the
  broker embedded, and npm broker binaries split into per-platform
  optional-dependency packages — the binary-resolution bug class that had
  generated traffic every month since December is gone.
- GitHub/browser workflow primitives; PostHog v5 with per-component versions;
  relayfile-mount auto-download; .agentignore/.agentreadonly enforcement.

## engine (relaycast)
- Quiet month (15 commits): system identity/messages, posthog-node migration,
  relaycast CLI as its own package, shared A2A schema package. v1.1.2–v1.1.5.

## files-auth
- Mount daemon matures: @parcel/watcher swap, abortable shutdown sync,
  subscription-leak fix, one-way .git sync; fork API; contentIdentity enabling
  server-side dedup; per-file revisions.
- **RS256 migration with no compat shim**: Go server accepts RS256 via JWKS,
  then the HS256 path is deleted outright. relayauth ships token/api-key
  endpoints, D1 migration runner, SSE auth-event dashboard.
- Adapters: SchemaAdapter migration campaign; canonical query/operation
  builders for GitHub, Linear, Notion.

## agent-platform
- **agent-workforce CLI born 04-17**: persona list, harness detection, dev
  watch, skills staging. Persona skills + usePersona().execute().
- **04-30 pivot: persona API becomes install-only**; umbrella agentworkforce
  package; OIDC trusted npm publishing.
- relayflows: browser + github primitives, credential-proxy runtime,
  ProcessBackend for cloud sandboxes, fast workspace seeding, RS256 minting.

## clients (pear)
- Cloud auth + cloud broker support, then a 7-week pause.

## infra-web
- cloud (372): specialist workers (Linear, Notion, VFS enumerate), cataloging
  agent, **Sage cut over to a Cloudflare queue+DO runtime** (killed the
  silent-Slack-reply bug), relayauth D1 migrations, CI binding validator.
- relaycron rewritten and renamed @agentcron → @relaycron; Cloudflare deps
  stripped from the server package (local-first Node + SQLite); v0.1.1/v0.1.2.
- **burn born 04-20**: readers for Claude/Codex/OpenCode, ledger, analyze,
  MCP; 1.2.x → 1.3.1 lockstep releases.
