---
month: 2026-07
backfilled: 2026-07-29
repos: [relay, relaycast, relaycast-cloud, relayfile, relayauth, relayfile-cloud, workforce, agents, internal-agents, pear, chief-app, cloud, agentrelay.com, burn, skills]
---
# 2026-07 — node providers, hardening, kits, and the chief restart

## core-messaging (relay) — v9.2.x → v11.2.0, 476 commits
- **v10.0.0 node providers**: a node's actions hosted by providers connecting
  directly to the engine; the broker becomes one provider among several,
  self-advertising spawn:<harness> capacity; actions authored in TS
  (defineNode/serveNode) or Python.
- v10.2.0 deep PTY-correctness: dead-letter queue + redeliver, persisted
  dedup, input acks confirmed by the child, non-blocking write drainer,
  interactive holds, watchdog that stops killing thinking agents.
- v10.5.0 fixes a product-breaking bug (driven agents could never receive
  relay messages); adds Ctrl+] held/live toggle and **SDK observer mode**.
- **v11.0.0: AI SDK harness adapters become the native runtime** (Claude
  Code, Codex, OpenCode, Pi; --runtime pty retained). Node 22+.
- v11.2.0: Cloud multiplayer **rooms** (secret-sink invites, revocable
  per-device sessions); CLI integration catalog.
- Feature-guardian verification relayflow: 124-feature manifest verified
  tier-by-tier, PASS/FAIL posted to #relay-health.

## engine (relaycast + relaycast-cloud)
- v5.1.0 durable workspace event log + scoped ot_live_ observer tokens;
  v5.1.1 relayfile inbound ingress to node agents.
- **v6.0.0 multi-provider nodes**, then two weeks of delivery-durability
  hardening (v6.0.1–6.0.5) closing the silent-delivery-loss bug class
  (sequences, cursor negotiation, sweep redrive, fail-fast handlers).
- v6.2.0 durable agent.exited/node.status events; disconnect presence-only
  for node-hosted agents. v6.3.0 caller-declared machine/user/org telemetry
  identity — per-person and per-customer usage reporting.

## files-auth
- **Concurrent editing arc (#370–#377)**: stale-write detection, fork rebase,
  symbol-level merge for Go, language-agnostic three-way merge, fuzz + chaos
  tested, two-machine collaboration proof — the VFS becomes a genuine
  multi-agent collaboration surface.
- Relayfile Path Contract v1 (grammar, fixtures, conformance runners).
- relayauth v0.2.22: retention GC, rate-limit cardinality isolation.

## agent-platform
- **CLI warm fast path**: cold launch 26s → 2.5s, warm ~20ms, teardown <1s.
- **07-15 contracts split**: @agentworkforce/events, versioned Run contracts,
  compose (portable TeamSpec), local-surface (fleet-node bridge — proactive
  personas on a laptop), turn-kit (multi-turn lifecycle), review-kit,
  deriveAgentCard() → A2A agent cards.
- internal-agents: reddit-monitor production + deploy-on-merge CI; agent-duet
  canary hardened. Cross-cutting theme: mount-scope tightening + non-vacuous
  acceptance evidence.

## clients
- pear: terminal fidelity as a test discipline (real Electron matrix,
  divergence corpus); reconciler correctness; observer-stream consumption;
  fleet placement to any node; relay v9.2 → v11 march. v2026.7.1, .7.3.
- **chief-app: M0 (chief on relay, 07-03) → M1 (Mac chat, 07-05) → M2 (live
  roster/streams, 07-05) in three days** — then hit the relay v10
  add_agent-dispatched-but-no-PTY wall; pivoted uncommitted to headless
  claude -p via a chief.message action; parked 07-14.
- **07-29: chief restarts app-less in the `chief` repo** — markdown brain
  (memory/journal/workstreams), /retro /status /digest skills, platform
  mechanics pushed to the owning components.

## infra-web
- **Phase 5 (07-13): cloud-web Lambda, CloudFront router, and the entire
  VPC/NAT/bastion/ECS footprint deleted** — always-on NAT cost gone across
  prod/dev/staging. #1967 Phase A: SNS OpsAlarms removed, retention cron
  moved to sst.cloudflare.Cron.
- cloud feature work: Factory hosted control loop + per-run cost ledger,
  relayauth dormant delegation, Telegram multi-bot routing, Herdr rooms +
  secure email invites, dynamic agent one-shot schedules; managed cloud
  schedules enabled 07-28.
- agentrelay.com (07-19): Factory docs section, integrations catalog, dense
  agent-gallery rows, /will and /khaliq meet aliases.
- burn quiet after 07-03 (macOS in-app updates, Swift 6 fixes).
