---
status: complete
owner: chief
updated: 2026-08-11
repos: [relay, relaycast-cloud, relayfile, cloud, c2a, relayauth, factory, relayscribe, chief]
---
# Handoff — 2026-08-11 — resident Chief replaced

**This controlled replacement is complete.** Per `CLAUDE.md` §7, the prior
rostered resident (`chief`, `teams.json`, host `chief-broker` / this local Mac,
session `94c392bf-ec18-459a-95b6-440dd56de94c`) wrote this handoff and was
released. A fresh process now holds the exact canonical `chief` identity under
session `07c4b6a0-ee38-4152-b6ed-be74015a6bfb`; its process and live tool
activity were verified before the temporary shadow was released.

**Do not confuse the resident with the second Chief already active tonight:**
`chief-proof-coordinator-0811` is a bounded, temporary Chief on `finn-mini`
(invocation `inv_212997475883134976`, node
`node_d4190c4c2ca5c26bf547301347af4028`), coordinating a 4-lane cross-host
proof team. It is not the resident and must not become it. Full detail:
[[active-lanes]].

## Unsafe to release — read before doing anything else

1. **`relayscribe#9` disclosure — UNRESOLVED, public repo.**
   `AgentWorkforce/relayscribe` is public. The open spec PR (branch
   `docs/recorder-auth-spec`, `08ff21a`) names the exact path a shared secret
   is extracted from in every shipped `.app`, and states one `grep` against a
   downloaded DMG yields a working credential. **The secret value is not
   printed, the recipe is, and rotation has not happened — the credential is
   live.** Standing rule: exploit paths go in `sales` only. Do not merge, do
   not further publish extraction detail. Options are recorded in
   `journal/daily/2026-08-11.md`; **decision is Khaliq's**, still open.
2. **Live exposed credential values in an orchestration transcript.** A
   diagnostic host process listing printed Relay credential values during
   tonight's cross-host proof work. Both Chiefs were told not to print or
   reuse those values outside the sessions that saw them. Workspace-key and
   affected agent-token rotation is **deliberately deferred** until every
   active proof/publication lane has a durable checkpoint and reconnect
   path — do not rotate mid-step, but do not let this go unrotated once the
   proof lanes close.
3. **`opencode.json` credential exposure — RESOLVED, do not re-raise.**
   Khaliq ruled 2026-08-10: `chief` is private, exposure accepted, no
   rotation. Squash-only merge is the mechanism keeping `261db56` off `main`
   (repo is set squash-only for exactly this reason — do not change that
   setting without knowing why).
4. **SOC-2 / Nabis material never goes in a public repo.** Customer name,
   headcount, the Nov-1 auditor date, the Hole 1 exploit path, and any leaked
   key must never appear in `factory`, `relay`, or `relayauth` (all public).
   Source of truth is private: `AgentWorkforce/sales#27`.
5. **Merges require explicit principal authority.** Khaliq granted autonomous
   merge authority for the currently scoped attach, Daytona, Relayfile
   anti-storm, and proof work. Required review, CI, Veto review when available,
   and safety gates still apply. Items outside that scope still wait for an
   explicit merge decision.

## Active lanes — owner, host, status, evidence, blocker, next

| Lane | Agent / owner | Host / node | Status | PR · branch · commit | Blocker | Next |
|---|---|---|---|---|---|---|
| relay attach hop, phase 1+SSH | `fleet-attach-impl-0811` | finn-mini (`node_d4190c4c2ca5c26bf547301347af4028`) | active | relay#1480 merged; relay#1483 merged → `main` `ed8144c9a`, final head `986b90e77` | none on merged work; canonical `--node` phase 2 has no PR yet | implement fleet-native `--node` (registry placement + outbound ephemeral terminal tunnel); SSH stays fallback. [[cross-node-attach]] |
| Relayfile storm guard | `relayfile-storm-guard-0811` | Barry (`node_210867409538764800`) | active | relayfile#414 merged → `main` `7f95516d7206` | both launchd supervisors (`com.agentworkforce.chief.senses`, `com.agentworkforce.chief.integrations-mount`) still **disabled** — do not re-enable either without single-owner + canary | assign one supervisor, canary with resource telemetry, then land P1 items (telemetry, projection scoping, registry cleanup of triple `rw_7ccfea89`). [[relayfile-coordination]] |
| Daytona proof | `daytona-mount-proof-0811` (prior: `daytona-relayfile-closeout-barry-0811`, released) | node `node_212862301507432448`, sandbox `dedfeb9a-8682-4b89-957f-5bd15603ee0c` | **blocked** | cloud#2984 merged `16f58648e` (node online, phase 2 proven); cloud#2991 merged `5c90d2994` **not deployed** | production deploy run `31516360915` failed — SST secret `TranscriptionWorkerServiceToken` unset | production admin sets the secret + redeploys; then resume mount proof (Chief reappoints owner — do not touch until unblocked). 24h heartbeat gate due ~2026-08-12T09:22Z. [[daytona-fleet-nodes]] |
| relay#1476 conformance fixture | (c2a-lead-0811b swept threads) | — | review-ready | relay#1476, HEAD `8502323`, CI 7/7 + CodeQL green | **Khaliq required-reviewer approval** | Khaliq approve at github.com/AgentWorkforce/relay/pull/1476 |
| relaycast KV 503 fix | `relaycast-kv-lead-0811` (released) | — | **done** | relaycast-cloud#57 merged + deployed, run `31487818487`; prod confirmed 201 | none | closed. Root cause: `IDEMPOTENCY_LOCK_TTL_SECONDS=30` < Cloudflare KV 60s min, fixed via `Math.max(ttl,60)` in `kv.ts`. [[relaycast-kv-findings]] |
| relay#1479 | unowned | — | filed, open | relay#1479 | CLI sends `pathGlob`, server wants `path_glob` — blocks `agent-relay integration subscribe` | needs an owner |
| Relayfile GitHub webhook | `relayfile-subs-lead-0811` | — | active, subscription proven (`whsub_4304cfed` → `#github-pr-events`) | — | **webhook suspended since Aug 3**, workspace `rw_7ccfea89`, 27 dead-lettered envelopes, 18 process_failed, VFS missing PRs 1478/1479/1481/1482 | Khaliq: relayfile settings → GitHub integration → reconnect, or clear dead-lettered envelopes |
| pr-shepherd | `pr-shepherd-lead-0811v3` (reports `agent-coordination-lead-0811`) | — | active | deployed dry-run (`DRY_RUN=true`, no `SLACK_CHANNEL`); first cron tick 11:00Z | workstream doc's own "Now" section still reads "unbuilt" — **verify against the deploy before trusting either claim** | watch first cron tick, promote out of dry-run only after Khaliq reviews output. [[pr-shepherd-agent]] |
| c2a#4 spec gaps | `c2a-lead-0811b` | — | documented, DO-NOT-MERGE | c2a#4, 5 gaps filed as GitHub threads (obligationId regression, edit/delete schema, escalation resolution, silent drop, tool_mailbox) | ruling needed: agent proposes fixes, or hold for Khaliq's spec review | awaiting Chief/Khaliq ruling |
| SOC-2 traceability chain | `soc2-program-lead-0811` (session closed ~12:30Z) | — | **chain live in prod** | relayauth#79 + relay#1477 + cloud#2981 + cloud#2987 + cloud#2989 all merged; cloud#2989 admitted migration `0009_attestation_grants_session_ref` | none on the merged chain | 1) factory sessionRef forwarding — one-line change in `src/writeback/github.ts`, briefed to factory-lead, not yet a PR. 2) `findDuplicate()` always returns null while D1 empty — P2 data-integrity gap, separate cloud PR + triage ruling needed. 3) CH-1 re-scope awaits Khaliq yes/no (internal-only vs external IdP). 4) optional prod D1 baseline read on db `75960af9-277a-4b30-a63c-0b24bc598682`. [[soc2-agent-traceability]] — note: file's own frontmatter `owner: soc2-lead-0810` is stale, correct to `soc2-program-lead-0811` on next touch |
| factory#230 lifecycle | `chief-barry-codex-0811-1440` (released) | Barry | draft | Factory PR #232, `codex/factory-230-generation-cas`, `0024c6b0cf32595d19b9c24b8e6f8d73375fdd63`; proofs 2/2, suite 21/21 | none blocking review | needs a reviewer; lifecycle wiring/local claim-map integration/pending-spawn recovery still out of scope. [[factory-live-dispatch]] |
| Cloud identity D1 | unassigned (prior `cloud-identity-lead-0811v3`) | — | design phase | cloud#2981 landed the interim ledger-first-ordering fix; this lane is the follow-on | none blocking; needs an owner | write the design note: what DOs give today that D1 doesn't, before proposing to leave them. [[cloud-identity-d1]] |
| intent-trajectory-lineage | unassigned (prior `trajectory-lead-0811v3`) | — | doctrine captured, nothing built | — | none blocking; needs an owner | survey is done (trail 0.5.8, ai-hist, relaycast messages table, relayauth sessionRef all mapped); next is picking the first slice. [[intent-trajectory-lineage]] |
| chief-memory-encoding | unassigned (prior `trajectory-lead-0811v3`) | — | ruling captured, nothing built | — | none blocking; needs an owner | Khaliq ruled memory logic/encoding lives in Chief; needs an addressable-unit design for memory files so trajectory pointers have a target. [[chief-memory-encoding]] |
| relayscribe recorder auth | unassigned (prior `relayscribe-lead-0811`) | — | spec written, unimplemented | relayscribe#9 (see Unsafe-to-release #1) | disclosure ruling (above) blocks merge; implementation itself is unblocked | fix worker/sidecar auth mismatch (401 on every request today), remove shared secret from client artifact, fix retry-exhaustion silent failure |

## Also still open, lower urgency

- `sage-nightcto-factory-program` — awaits Khaliq park-or-run.
- `cloud-org-appointments`, `agent-lifecycle-workflows`, `agent-event-subscriptions[-pattern]`, `helm-charts`, `workspace-convergence`, `yc-demo` — active per frontmatter, no news since their last dated entry; read each directly before assuming stale.

## Where to look for depth

This file is a roll-up, not a replacement for the per-lane workstreams linked
above, `journal/daily/2026-08-11.md` (full day narrative), or
`memory/open-threads.md`. Read those before re-deriving anything already
answered here.

## History

- 2026-08-11 — Controlled rotation completed: prior canonical `chief` released,
  replacement reacquired the exact canonical identity and demonstrated live
  work, then temporary shadow `chief-local-0811` was released.
- 2026-08-11 — Written on request as a controlled-replacement handoff. Resident
  Chief not released; orchestrator to verify successor against this file.
