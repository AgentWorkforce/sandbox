### #2917 phase checkpoint — bounded Queue canary V3 tooling open / no production mutation

PR [#2968](https://github.com/AgentWorkforce/cloud/pull/2968) is open at pushed head `7eb0075a6`.

The protected workflow has distinct read-only `preflight` and mutation-gated `execute` modes. Execution cannot start unless it re-reads both the exact preflight control HMAC and an issue comment whose pinned ID/body SHA contains the literal `#2917-QUEUE-CANARY-V3` amendment.

The bounded path:

- keeps the DLQ paused throughout;
- rotates only the four dormant Worker admission bindings;
- changes the main consumer with a full-body update to batch 10 / maximum concurrency 1;
- resumes only the main queue, polls the durable D1 admission row to exactly 10 within 120 seconds, then immediately re-pauses;
- requires a live-tail probe before mutation and accounts for all 10 consume/terminal dispositions;
- stops and contains on any retry, 5xx, DLQ movement, RelayAuth health drift, excessive D1 growth, ownership drift, or loss of observability;
- restores main pause, the exact base consumer settings, and a dormant gate version on every handled exit.

Raw resource/message identities and tail events are written only as AES-256-GCM evidence. The source has no DELETE, purge, DLQ replay, or Nango mutation path. Local checks are green: 7/7 focused tests, Node syntax, Prettier, diff check, and manual mutation/secrets review. Full CI and automated review are pending. Veto MCP is unavailable in this lane, so no Veto pass is claimed.

No production resource was mutated in this phase. The protected read-only preflight will not run until the PR is fully reviewed and merged safely.
