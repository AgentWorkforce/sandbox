### Canary V3 exact-admission correlation checkpoint — no production mutation

PR #2968 is pushed at exact head `3e2eefcd29f822c2f15f60004ef8e28316ecb121`. The two latest P1 threads are fixed, answered, and resolved; unresolved count is currently 0.

- After the encrypted cleanup capsule, execute freshly proves both main and DLQ paused before the atomic D1 claim, then proves both paused again immediately before Worker gate activation. Drift enters cleanup before Queue resume or consumer relaxation.
- `webhook-worker` now emits one additive `queue_admission_message_admitted` record for every durably admitted message, containing the canary run ID and message ID.
- The controller builds the exact run-admitted message-ID set, requires 10 unique IDs matching the durable/tail admission totals, and ignores every consume/terminal event outside that set.
- Terminal ACK still additionally requires post-dedup `message_processing_completed`; retry/error overrides remain sticky.

Focused result: 16/16 controller tests pass; the Worker admission test asserts exactly the admitted prefix receives run-correlated telemetry. Node syntax, Prettier, and diff checks pass. Local Worker Vitest/typecheck binaries are unavailable in this worktree, so those results are not claimed; full CI is restarting and is required.

This additive Worker telemetry requires a controlled production Worker deploy and live Queue/gate readback after merge and before canary preflight. No deploy or other production mutation has occurred yet. The amendment remains unposted and execute remains undispatched.

Veto MCP is unavailable; no Veto pass is claimed. CodeRabbit remains rate-limited and contributes no pass.
