### #2917 phase checkpoint — registered-vs-scheduled Nango reconciliation open / no mutation

PR #2966 is open at pushed head `b17963f7fd2a918dc00fe00f8612478a55f5eb42`.

This read-only probe separates two facts the earlier manifest conflated:

- **scheduled connections**: unique provider/connection tuples represented by the 109 live schedule rows (currently 28);
- **registered connections**: live Nango connection registrations, including any with zero schedules.

It reports registered-without-schedules and scheduled-without-registration independently, plus provider-level registered/scheduled/schedule/paused/active aggregates and identifier-free SHA-256 digests. Exact connection membership remains only in the protected full-manifest path; inventory artifacts, console output, workflow comments, and errors contain no connection IDs.

Targeted Node tests, Prettier, Node syntax, and diff checks pass. Coverage proves registered-vs-scheduled reconciliation, provider aggregates, identifier redaction, and GET-only execution. The branch is pushed; no production workflow or mutation was executed.

Veto MCP remains unavailable; no Veto pass is claimed. Full applicable CI, review, thread resolution, and manual security/secrets review are pending before merge.
