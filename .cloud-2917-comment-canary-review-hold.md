### Canary V3 review HOLD — five findings / no production mutation

Exact-head Codex review on PR #2968 returned five substantive findings. Merge and preflight are held:

- P1: in-process `finally` is not sufficient for workflow cancellation/job timeout; cleanup must also run independently of the control process.
- P1: an ambiguously successful Worker settings PATCH can publish a gate-enabled version after the initial bounded poll; restoration must reconcile/neutralize late owned versions before claiming dormant state.
- P1: an early Cloud Web forward ACK precedes durable dedup completion; terminal success must be based on a later post-processing signal or a proven settle/recheck.
- P2: a lost dormant-deployment response must accept readback proving the owned dormant version is already active.
- P2: `dlq.remainedPaused` and the durable text must derive from observed readbacks, never a hard-coded `true`.

These findings are accepted. PR #2968 is not eligible to merge until all five are fixed, tested, the review threads are resolved, and full CI passes on the corrected immutable head. Veto MCP is unavailable; no Veto pass is claimed.

No production Queue, DLQ, Worker, Nango, D1, alert, or secret mutation occurred. The amendment remains unposted and execute remains undispatched.
