### #2917 Queue canary V3 enable-PATCH checkpoint — HOLD

- PR: #2968; exact pushed head: `d85d1498d2751961223cb71e28b31858753d5255`.
- Fixed the enable-PATCH P1: immediately before the settings PATCH, enable now runs the same <=5s all-settled proof for base active ownership, exact latest `base.version.id`, and both Queue holds. The later candidate deployment repeats the proof against exact latest `candidate.id`.
- Both `inherit latest` settings mutations - enable and dormant restore - are therefore boundary-bound before production mutation.
- Focused verification: 22/22 combined controller + snapshot tests green; syntax, Prettier, diff, mutation allowlist, redaction, and manual security review are green.
- The review thread was answered/resolved. Full exact-head CI and exact-head review are pending, so this remains HOLD.
- No production Queue, Worker, D1, DLQ, or Nango mutation occurred. The amendment is not posted and execute is not dispatched.
- Veto MCP is unavailable in this lane; no Veto pass is claimed.
