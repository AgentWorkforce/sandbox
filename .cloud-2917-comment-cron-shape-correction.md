### #2917 correction — cron RED is an instrumentation shape mismatch / no mutation

The successful run `31280246824` established:

- both queues still `delivery_paused=true`;
- one fully resolved worker consumer on each queue, both attached to the same production Worker;
- expected main/DLQ consumer settings and DLQ linkage;
- dormant admission gate (`enabled=false`, no run ID);
- unchanged Nango counts at 109 / 28 / 91 / 18.

However, its `Queue-health cron readback: RED` is **not valid evidence that the cron is absent**. Cloudflare's documented GET-schedules response is an object shaped `{ schedules: [...] }`; the snapshot currently tests `result` itself as an array. The test fixture masked this by returning an array directly.

Therefore the cron state is corrected from RED to **UNKNOWN pending lossless readback**. No cron PUT, SST deploy, Queue change, or other production mutation will be attempted on this false negative.

Next read-only amendment: normalize both documented object and legacy array response shapes, change the fixture to the documented object, and publish the redacted schedule count/expression/digest. Only a verified post-fix readback can authorize or rule out a narrowly scoped cron repair.

No event was purged, deleted, replayed, or dispositioned.
