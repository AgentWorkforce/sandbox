### #2917 encrypted exact-evidence prerequisite open — no production mutation

PR #2967 is open at pushed head `6479876cc2d4755df833ecf187c641f65d580eef`.

- Full mode now requires exact counts and three keyed pins from a preceding stable inventory: immutable schedule membership, registered membership, and PAUSED/frequency control state.
- Missing pins or a missing/invalid canonical 32-byte evidence key fail before any network read; mismatched live pins fail before any evidence write.
- Exact snapshot/manifest plaintext is encrypted in memory with AES-256-GCM using independent random IVs and authenticated AAD, self-decrypted for verification, then only `.enc.json` envelopes are written.
- Raw SHA digests, provider/connection identifiers, immutable request bodies, and database/resource identifiers remain inside encrypted evidence. Public output contains only keyed HMACs/pseudonyms and non-secret encryption metadata.
- Tests decrypt valid evidence, reject tampering, prove plaintext files are absent, prove missing/wrong pins fail closed, and retain GET-only/redaction/stability/duplicate safeguards. Targeted Node test, Prettier, syntax, and diff checks pass.

The production evidence secret has **not** been created and full mode has **not** been dispatched. No Queue, DLQ, Worker, Nango, D1, alert, or secret mutation occurred.

Veto MCP remains unavailable; no Veto pass is claimed. Full CI and review are pending. HOLD remains in force.
