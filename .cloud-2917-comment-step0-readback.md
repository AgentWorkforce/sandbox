### #2917 Step 0 readback — holds intact; earlier zero-consumer conclusion corrected

Protected inventory run `31279106442` completed successfully from post-#2956 `main` at `2f7ee9dafcfce14a58d348b9e2e38c7fc1b4ff3d`. The downloaded artifact is access-controlled, GitHub-pinned at SHA-256 `006e145030c6835c2394462e43051dadbf8e755b5aa8402a907252dc51b75754`, and its internal `SHA256SUMS` validates.

**Live production readback (2026-08-08T21:21:46Z):**

- `webhook-events`: 14,725 messages / 245,979,492 bytes; `delivery_paused=true`.
- `webhook-events-dlq`: 373 messages / 8,343,627 bytes; `delivery_paused=true`.
- Both the dedicated consumers endpoint and Queue-detail view report exactly **1 consumer on each queue**, and their redacted type/DLQ/settings observations agree.
- Main settings: worker; DLQ target `webhook-events-dlq`; batch 50; max retries 5; max wait 5,000 ms; retry delay 0.
- DLQ settings: worker; no downstream DLQ; batch 50; max retries 1; max wait 5,000 ms; retry delay 0.
- RelayAuth health/JWKS 200/200 with one key; replacement D1 growth 0 bytes over 10 seconds; rollback D1 count 36.
- Nango is unchanged from the 09:47Z hold: 109 schedules / 28 connections / 91 PAUSED / 18 active. The independent historical custody baseline remains 109 / 30 / 108 / 1, so the two Nango deltas remain unreconciled and no Nango mutation is authorised yet.

**Correction to the preceding zero-consumer checkpoint:** run `31277551851` failed after deriving unique Worker names from the optional `script_name` field; it did **not** prove that the endpoint list itself was empty. The new independent count cross-check proves both queues have one consumer. No consumer restoration is being attempted.

Cloudflare documents `script_name` as optional on the list/get Consumer schema. The live list/detail objects omit it, so Worker identity, admission-gate state, and cron remain **UNKNOWN**, not healthy. The next read-only amendment will GET each existing consumer by its durable consumer resource identifier inside the protected workflow and emit only the redacted Worker name/configuration—never the identifier—to determine whether the list omission is lossy. Until then Step 0 remains RED/hold.

No Queue, DLQ, Nango, Worker, D1, or alert mutation was performed by this lane in this phase. No event was purged, deleted, replayed, or dispositioned.
