### #2917 Nango evidence duplicate-invariant checkpoint — no production mutation

- PR #2966 immutable head advanced to `3580c3b0fd4d414b54bc99c86ae17505e4c4aa4f` before the restarted CI completed.
- Adjacent fail-closed hardening: integration records must contain valid unique keys; registered connection tuples and schedule tuples must be unique. Inventory-only mode no longer silently filters malformed integration rows or collapses duplicate membership into sets.
- Negative tests prove malformed integration rows, duplicate registered connection tuples, and duplicate schedule tuples abort the capture. All prior pagination, shape, stability, GET-only, and redaction coverage remains green.
- Targeted Node test, Prettier, Node syntax, and diff checks pass. Full immutable-head CI/review restarted.
- Veto MCP remains unavailable; no Veto pass is claimed. No Queue, DLQ, Worker, Nango, D1, alert, or secret mutation was performed.

HOLD remains in force pending all gates on `3580c3b0fd4d414b54bc99c86ae17505e4c4aa4f`.
