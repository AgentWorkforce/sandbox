---
date: 2026-07-30
repos: [chief, relay, cloud, agentrelay.com, cso, cpo, cmo, mobile, relayfile, relayauth]
tags: [chief, restart, workspace-key, factory, cso-watchlist, digest]
---
# 2026-07-30

## In flight (pre-restart handoff, ~15:40Z)
- Will is executing a full fleet stop/restart. Successor chief: run the
  post-boot sweep — health + inbound proofs (outbound-evidence standard)
  + nudges per the deaf-roulette expectation (#1386); the burn-node
  input-dead specimen (pid 95573) dies with the restart — condition is
  reproducible on fresh spawns if the platform team still needs it.
- Open deliverables riding through the restart (all durable in owners'
  standing boards): relay's release package + pending-count verdict +
  #1386 RFC; chief-app walking-skeleton date w/ intent v3 (local-first,
  login-free); mobile defect fixes; burn seat restoration + factory
  issues #503-#507; Khaliq onboarding handoff (docs/onboarding-khaliq.md).
- New session-recycling policy (≤4h, assignment boundaries) is
  effectively initialized by this restart — all seats come back with
  fresh contexts.

## Post-restart sweep, part 1: workspace split-brain found and fixed (~19:30Z)
- Fleet came back 19:21Z — 15 brokers, all started 15:21:24 EDT. Fourteen
  on the org workspace (`rk_live_d546…`); **chief alone re-homed to the
  stale July-11–15 probe workspace (`rk_live_370d…`)**, voice riding
  along. The org was reporting into d546 with chief absent.
- Cause: the global workspace store (`~/.agentworkforce/relay/
  workspaces.json`) had `active: "chief-dev"` → 370d (mtime 09:19 same
  morning); `node up` preferred it over the repo state dir's d546 and
  rewrote the state dir at boot (15:21:28). Department repos keep their
  own state-dir keys, which is why only chief flipped. Local sibling of
  the `--workspace-key not honored` family on #1386.
- Fix executed before this restart: store gains entry `org` → d546 and
  active switches to it; chief's repo state-dir `workspace-key.json`
  restored to d546 (both sources aligned = deterministic boot); node
  kickstarted via launchd (clean env per learnings). This entry is
  written pre-kickstart — the restart kills the writing session by
  design.
- **Successor checklist:** (1) workspace proof — `list_agents` must show
  the department residents (cso/cpo/cmo/burn/…), not the probe-era
  roster; (2) run the pre-restart sweep above (health + inbound proofs
  + nudges per the #1386 deaf-roulette expectation); (3) departments
  booted 19:21Z and may have ACK'd chief into d546 during the gap —
  inside mailbox TTL if fast; ask leads to re-report if replay looks
  thin; (4) DM the Relay Product Owner to file: `node up` lets the
  global workspace store override an existing per-repo
  `workspace-key.json` (concrete repro on this machine, log at
  `~/Library/Logs/chief-node.log` shows d546×4 then 370d).

## Post-restart sweep, part 2: fleet back, roster proven (~00:50Z Jul 31)
- **Successor: read `../cpo/escalations.md` before the 36-message
  thread.** It is cpo's current ledger of everything outstanding and with
  whom — rotation, retention, GitHub Team, per-agent identities,
  second-operator, watchdog, parent-file items, four dead seats. The
  thread is reasoning only.
- Workspace proof PASSED: `list_agents` returns the department org roster
  (cso/cpo/cmo/burn/relay/…, heads, watchdog, voice) — chief is on the
  org workspace, not the probe one. Part 1's split-brain fix held.
- Inbound proofs (outbound-evidence standard) arrived unprompted from
  relay (ACK+DONE), burn, agentrelay-com (DONE, PR #37), relayfile,
  hoopsheet, cmo, notesnumber, cpo, cso, head-of-experiments. One-line-ACK
  nudge posted to #general for cloud, chief-app, mobile, relaycron-cloud,
  head-of-ecosystem (scout proven via cmo). Roster `status` flags are
  unreliable (relay marked offline while DMing) — evidence over status.
- Watchdog BROKER_DOWN×15 "recommend kickstart" assessed STALE with log
  evidence: the sweep ran 19:21:19Z, mid-boot (brokers started 15:21:24
  EDT = 19:21Z); the 19:31Z and 00:47Z sweeps are green. No kickstarts
  executed. Open-threads: needs restart-awareness / confirming re-scan —
  ×15 false "recommend kickstart" turns dangerous the day the watchdog
  stops being alert-only.
- **Rotation (cpo escalation row 1) disposition:** the disclosed
  `rk_live_`/`at_live_` pair is the exact class Will ruled on 2026-07-30
  (relay keys low-sensitivity; periodic refresh, not emergency rotation).
  The zero-cost stop-window closed 19:21Z. Policy-conformant close: chief
  schedules the first periodic workspace refresh at the next natural
  fleet boundary. cpo is independently surfacing rotation to Will via its
  session output — noted, not blocked.
- **relay#1397 assigned to relay as its first item**, ahead of
  release-package resumption (six shipped surfaces instruct agents to
  print a live workspace key). Release-package priority added from cso's
  finding: published CLI 11.3.0 `node up|status` prints an unmasked
  `rk_live_` (status also a clickable observer URL); the fix (#1380)
  merged 17:02:45Z but is in no release — the package should ship it.
  relay also files: `node up` global-store override (part 1 repro),
  spawned-session PATH missing `/opt/homebrew/bin`, GITHUB_TOKEN
  public_repo-scope override masquerading as "repository not found"
  (cmo/scout), and the standing secrets-redaction items.
- **Liveness-evidence carry (cpo 69b95aa): the restart destroyed the
  evidence of the detection gap, not the gap.** Four seats died today,
  every one found by a human-shaped question, none by the watchdog;
  post-restart the roster looks clean, so a successor has no reason to
  believe the gap exists. It does. Durable asks (broker-native liveness,
  per-agent identity for attribution) live in cpo/escalations.md and
  open-threads.
- **Recycled seats re-arm retired practices** (head-of-experiments):
  relayfile's fresh instance resumed the retired 15-min pull cadence from
  its durable state — a #general announcement reaches only instances
  alive to hear it. Promoted to learnings; leads told to sweep their
  durable files.
- **"CodeRabbit green" ≠ a recorded review** (cmo's correction): its
  audit found zero qualifying reviews on cmo#4, scout#16, and
  agentrelay.com#37 — the SUCCESS was a check context, no review ever
  submitted. Merge gates held; cross-review plan approved (cmo→scout#16 +
  agentrelay.com#37, scout→cmo#4; a re-triggered CodeRabbit counts once
  it actually submits; if it doesn't on cmo#4, the second review comes
  from outside GTM since merge deploys prod). Fleet broadcast: verify the
  review record (`gh pr view --json reviews`), never the checks rollup.
- cmo compliance sweep (ee7af50): prod-touching npm scripts (`deploy`,
  `db:migrate:remote`) removed per the no-prod-access law; README
  re-marked prod steps as human runbook; no credential-printing
  directives found.
- **cso watchlist seed pass green-lit** on its stated bounds (6–8
  dossiers from public sources, triage verdicts, single session, no
  fan-out). Right reframe: the senses mount blocks recovering
  already-shared links, not the research mandate — the corpus becomes a
  merge, not a cold start.
- Creator-partnerships split approved (cmo): strategy → cmo docs; the
  78-account list + outreach → scout campaign with contact data in
  scout's CRM store, not a committed CSV; authorship of the untracked
  material flagged to Will as a non-blocking FYI.
