---
month: 2026-06
backfilled: 2026-07-29
repos: [relay, relaycast, relaycast-cloud, relayfile, relayfile-cloud, relayfile-adapters, workforce, agents, internal-agents, relayflows, pear, cloud, agentrelay.com, burn, skills]
---
# 2026-06 — fleet delivery, the v8 split, Gate 3, and the software factory

## core-messaging (relay)
- **v8.0.0, largest breaking release of the window**: @agent-relay/sdk
  rewritten as a pure communication surface; all broker/PTY/spawn machinery
  moved to the new **@agent-relay/harness-driver**. State moves
  .agent-relay/ → .agentworkforce/relay/. agent-relay mcp ships as stdio.
- v9.0.1 removes the local web dashboard (web moves to agentrelay.com);
  v9.1.0 drops every legacy relaycast.dev reference — **cast.agentrelay.com
  only**.
- **Fleet begins**: node control plane in the broker, node sidecar protocol,
  heartbeat roster, placement.spawn({capability, node, repo}) with capability
  gating. Durable delivery wired to the relaycast backend.

## engine (relaycast + relaycast-cloud)
- **Durable delivery inbox** (ack/fail/defer, idempotent transitions) → v2.0.0;
  telemetry attribution (origin_actor) via i.agentrelay.com; v3.0.0 atomic D1
  writes + durable webhook outbox + Swift SDK.
- **Fleet delivery RFC executed in six numbered phases** → v4.0.0; then
  **v5.0.0: nodes become first-class delivery hosts** (direct WS, fleet WS,
  HTTP-push) with agent-node bindings and delivery retries.
- **relaycast-cloud carved out of `cloud` onto Cloudflare — AWS removed from
  SST state entirely**; retired direct-agent DOs deleted; legacy hosts kept
  serving behind a strangler router.

## files-auth
- Writeback becomes durable: ACKed outbox + adapter-dispatch receipts (ack
  only after the provider accepted the op).
- Auth unification: relayfile login delegates to agent-relay cloud login;
  path-scoped fs grants enforced for mount tokens.
- **Gate 3 prod cutover signed GREEN (06-20)** — DO state proven byte-intact,
  auth verified; gate workflows deleted 06-25. relayfile now runs
  file.agentrelay.com as normal infrastructure. (Mount round-trip leg waived —
  test-config gaps, read path confirmed.)
- Adapters webhook-fidelity pass: full-record refetch on ingestion across six
  providers; Slack server-side threading + idempotency keys.

## agent-platform
- **06-01 the persona/agent split**: triggers/schedules/watch move to
  agent.ts via defineAgent() — persona says what an agent IS, agent.ts when it
  fires. **06-10 v4.0.0**: handler events become the relay SDK's AgentEvent;
  declarative `relay` persona field; downstream repos codemodded same day.
- ctx.relay messaging, @agentworkforce/delivery, workforce invoke
  fixture-replay simulation, decision-trajectory recording, grok harness.
- agents: eval harness across all 10, real multi-member teams, pr-reviewer
  hardening (merge-on-green, VFS-based PR state). internal-agents: runtime
  4.x migration, x-reply-radar + the Slack threading saga. relayflows v1.0.0.

## clients (pear — 446 commits, the dominant month)
- **v1.0.0 (06-03)** signed + notarized macOS pipeline, auto-update.
- **Software factory** (~80 commits): Linear/GitHub issue → scope → team spawn
  → implement → PR babysitter → auto-merge gate; extracted to the published
  **@agent-relay/factory** package, in-Pear daemon torn down.
- Integrations reliability: mount respawn-storm cap, streams surviving token
  refresh, Slack DM pickers + thread context. Grok + OpenCode harnesses.
- Audit sweep 06-10: ~10 parallel audit/* branches (broker split, Zod ingress,
  blocking typecheck + ESLint baseline).

## infra-web
- cloud's infra-heavy month (758): **production full-SST diff gate +
  fail-closed delete approvals** — destructive infra changes can't slip
  through CI; two-phase SST deploy for cold bootstrap; **admin app + dormant
  AWS Lambda web origin decommissioned**; Nango SQS+Lambda path removed for
  the CF Workflow; relay-on by default (every agent addressable at @self);
  all proactive agents onto cast.agentrelay.com.
- **agentrelay.com: router claimed the apex** and moved non-prod hosts off
  agentrelay.cloud — one front door; IndexNow on deploy.
- **burn 4.0.0** (breaking API tightening across 11 packages) + the macOS
  menubar app: live burn-rate chart, provider overlays.
