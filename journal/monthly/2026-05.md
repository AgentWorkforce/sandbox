---
month: 2026-05
backfilled: 2026-07-29
repos: [relay, relaycast, relayfile, relayfile-adapters, relayauth, workforce, relayflows, agents, internal-agents, pear]
---
# 2026-05 — deploy v1, the engine extraction, and the mount reckoning

## core-messaging (relay)
- **v6.2.0 attach triad** — view (read-only PTY), drive (take-over that queues
  relay messages), passthrough (auto-inject) — the core human-in-the-loop
  surface; broker-owned agents (new/rm), per-agent delivery modes.
- v7.0.0 breaks the SDK event API: typed multi-listener registry; wire
  protocol v2. OpenClaw line ends at 7.1.1.

## engine (relaycast)
- **@relaycast/engine extracted as a portable package** (05-29) with an
  action-contract E2E suite; four rounds of adversarial review fixes.
- **Legacy @relaycast/server deleted** (05-31) — the engine is the product.
  Apache-2.0 license lands: the OSS position. Web crypto for Workers compat.

## files-auth
- **The mount data-loss reckoning**: mount-root clobber exposure answered in
  layers — defense-in-depth, then structural invariants making the bug class
  unrepresentable, plus 10 clawpatch security findings fixed.
- Large-workspace convergence campaign: resumable pull, export caps with tree
  fallback, incremental checkpointing, workspace_busy backpressure.
- Scoped access lands: path-scoped tokens (relayauth) + scoped mount paths.
- Adapters breadth explosion (163 commits): trigger catalog as first-class
  export; Granola, X, Fathom, Dropbox, Docker Hub, Reddit, HubSpot, Gmail.

## agent-platform
- @agentworkforce/persona-kit extracted (v1.0.0); **deploy v1 (05-12):
  persona-as-deployable-agent** — tiers dropped, daytona-runner in-repo.
- 05-13 batch: mcp-workforce MCP server, JSON Schema export, --mode cloud,
  **Relayfile-VFS adopted as the canonical integration-client style**.
- **relayflows extracted from relay (PR #1000, 05-26)** — relay left with zero
  workflow code.
- **agents repo born 05-26** (six showcase proactive agents, launch badges);
  **internal-agents born 05-26** (Workforce → Team → Agent → Trigger;
  linear-assistant).

## clients (pear)
- Relaunch 05-19 (99 commits): burn ledger + per-session cost drill-down;
  PTY perf overhaul (dedicated IPC channel, decoupled buffers, O(n) dedupe);
  cloud agents + integrations + proactive managers; workforce persona
  spawning; IPC types consolidated under `satisfies PearAPI`.
