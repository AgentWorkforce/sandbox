### #2917 Queue canary V3 dormant-version checkpoint — HOLD

- Exact pushed PR #2968 head: `2a26303abca0a2866d5549f7c2115f5c554e35b6`.
- Proactively extended the settled boundary invariant to dormant version creation: immediately before the dormant settings PATCH, restore now requires the same <=5s joint Worker-ownership/main-hold/DLQ-hold proof and verifies the active Worker is run-owned. This contains a lost/ambiguous settings response that activates a newly created dormant version before the later explicit deployment step.
- Hardened amendment rendering further by rejecting non-ASCII confusables as well as entity syntax; the canonical amendment is intentionally ASCII-only.
- Combined controller + snapshot tests remain 21/21 green; syntax, Prettier, diff, mutation allowlist, redaction, and manual security review are green.
- Exact-head CI and review restarted. No production mutation; amendment unposted; execute undispatched. Veto unavailable/no pass.
