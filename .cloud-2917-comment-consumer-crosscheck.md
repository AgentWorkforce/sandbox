### #2917 phase checkpoint — consumer observation cross-check added / no mutation

PR #2962 head is now `8e751f9f2`. Before trusting the zero-consumer diagnosis, inventory now cross-checks the dedicated Queue consumers endpoint against the Queue detail response's independent `consumers_total_count` and embedded consumer view. The redacted checkpoint will report both counts and a consistency gate, preventing an API-shape or observation disagreement from being misreported as production drift.

The exact zero-consumer regression and all targeted checks re-pass. Full applicable CI/review restarted on the updated head. No protected branch run or production mutation occurred; recovery remains **HOLD**.
