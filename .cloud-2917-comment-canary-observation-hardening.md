### Canary V3 observation hardening checkpoint — no production mutation

Manual pre-merge review found two fail-closed evidence gaps in PR #2968. They are corrected and pushed at exact head `fc513c33a8c3f4d8428b36963a2f3193fbd3758a`:

- The DLQ pause invariant is now read back on every outcome-observation poll, not only before admission and at the end.
- Admission reservations now expose admitted and held totals. Any gate-held message is treated as retry amplification and forces `RED_CONTAINED`; observed admissions above 10 also stop immediately.
- A live-tail close failure cannot prevent the automatic restore path from running.
- Provider names and Worker consumer resource names are no longer present in the public evidence; provider references are keyed HMACs and exact control details remain encrypted.
- The durable issue checkpoint reports tail-side admitted/held/reservation accounting.

Focused result: 8/8 tests pass, Node syntax and diff checks pass. The source still contains no Queue purge/delete, DLQ replay/unpause, or Nango write path. Full CI and exact-head automated review have been restarted; no pass is claimed yet.

Both production queues remain under the previously verified hold because this phase changed repository code only. The `#2917-QUEUE-CANARY-V3` amendment has not been posted and the execute mode has not been dispatched.

Veto MCP is unavailable in this lane, so no Veto pass is claimed. Equivalent local checks in this increment were focused unit tests, syntax validation, formatting, diff validation, and manual mutation/secrets review.
