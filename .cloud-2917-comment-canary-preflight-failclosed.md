### Canary V3 preflight fail-closed checkpoint — no production mutation

PR #2968 is pushed at exact head `088047b5816949cd1b019571b37695228d008fc3`.

An additional pre-merge audit found two prerequisites that were displayed but not enforced early enough:

- Preflight and execute now both require the durable `webhook_queue_admission_gate` table to exist. An absent table is RED before any Queue/consumer/Worker write.
- Both queues' backlog count and byte metrics must be complete, non-negative safe integers during the initial control snapshot. Missing/invalid metrics are loss of observability and fail before mutation.

Focused result: 9/9 tests pass, Node syntax, Prettier, and diff checks pass. Full CI and review are restarting on the new exact head; no pass is claimed yet. The written amendment remains unposted and execute remains undispatched.

Veto MCP remains unavailable, so no Veto pass is claimed. No production Queue, DLQ, Worker, Nango, D1, alert, or secret state was mutated.
