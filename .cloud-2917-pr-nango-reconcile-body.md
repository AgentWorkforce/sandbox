## Summary

- list registered Nango connections through the current read-only `/connections` API, with deprecated `/connection` fallback
- preserve the existing historical invariant as scheduled-connection membership while separately reporting registered connections
- count registered connections without schedules and scheduled connections without a current registration
- publish provider-level schedule/connection/paused/active aggregates plus identifier-free digests
- include exact registered connection membership only in the full protected manifest path; inventory artifacts/comments remain redacted

## Why

The verified live schedule manifest contains 109 rows across 28 connection tuples, versus the durable historical 30. That does not establish whether two Nango connections were deleted: they may still be registered but have zero schedules. This read-only distinction is required before any Nango state mutation or V3 canary.

## Verification

- `node --test tests/incident-2917-snapshot.test.mjs`
- `npx prettier --check scripts/incident-2917-snapshot.mjs tests/incident-2917-snapshot.test.mjs .github/workflows/incident-2917-production-snapshot.yml`
- `node --check scripts/incident-2917-snapshot.mjs`
- `git diff --check`
- test proves registered-vs-scheduled reconciliation, provider aggregates, identifier redaction, and GET-only execution

No production workflow was dispatched from this branch. Veto MCP is unavailable, so no Veto pass is claimed; full CI/review/manual security evidence is required before merge.

Relates to #2917.
