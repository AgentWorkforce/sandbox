### #2917 Queue canary V3 dormant-restore checkpoint — HOLD

- PR: #2968; exact pushed head: `233dc28ce2e988138e1484514a51bce57348e227`.
- Fixed the dormant-restore P1: every `deployReconciledDormant` call now requires the same joint active-Worker ownership + main hold + DLQ hold proof, no older than five seconds, before it may return already dormant or issue a dormant deployment POST.
- The proof callback is mandatory and fail-closed; absence rejects before POST. Each restore-time proof also updates truthful late main/DLQ readback and violation state. If a hold cannot be proved, the admission cap remains armed and automatic cleanup reports RED rather than relaxing containment.
- Focused verification: 20/20 combined controller + snapshot tests green; tests cover proof invocation, lost deployment-response readback, and no POST when the dormant proof is absent. Syntax, Prettier, diff, mutation allowlist, redaction, and manual security review are green.
- The review thread was answered/resolved. Full exact-head CI and exact-head review are pending, so this remains HOLD.
- No production Queue, Worker, D1, DLQ, or Nango mutation occurred. The amendment is not posted and execute is not dispatched.
- Veto MCP is unavailable in this lane; no Veto pass is claimed.
