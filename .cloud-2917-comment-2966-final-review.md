### #2917 Nango reconciliation final-review checkpoint — no production mutation

- PR #2966 new immutable head: `ef7a94f689fc7af68d29bb5a846d2fda41a1d07d`.
- The prior head completed full CI successfully, but the final automated review surfaced two P2 issues before merge; that head was not merged.
- Fixed and resolved:
  - schedule rows are now captured twice, and immutable tuple, PAUSED/active class, and frequency projections must match; a closing integration/registered-membership read brackets the second schedule pass;
  - raw provider configuration keys remain only in the exact protected snapshot; public issue/artifact summaries use keyed HMAC provider references with aggregate counts.
- Negative coverage proves schedule membership drift aborts. Public redaction coverage now explicitly rejects the provider key as well as connection/consumer identifiers.
- Targeted Node test, Prettier, Node syntax, and diff checks pass. Full CI/review restarted for the new head; zero unresolved threads at posting.
- Veto MCP remains unavailable; no Veto pass is claimed. No Queue, DLQ, Worker, Nango, D1, alert, or secret mutation was performed.

HOLD remains in force pending all gates on `ef7a94f689fc7af68d29bb5a846d2fda41a1d07d`.
