### #2917 Queue canary V3 — exact execute dispatched

The single bounded execute run is `31291010420`.

It was dispatched with the exact immutable amendment comment ID and body SHA-256, unique durable run value, preflight control pin, Worker identity pin, and maximum 10 from the posted amendment. Any drift fails before mutation.

The controller must report exact durable admission and exact terminal outcomes, then restore in process, on shell exit/signal, and in the independent cleanup step. **No success or restoration claim is made while this run is active.** DLQ and Nango exclusions remain absolute.
