### #2917 Nango evidence hardening checkpoint — no production mutation

- PR #2966 corrected head: `c96b6bdce58a0a3e29ddb54cdd1289c2d5ee25cf`.
- The corrected-head review identified four more evidence-integrity concerns; all are fixed, answered, and resolved:
  - registered membership and integration keys are double-read around schedule collection; any change aborts the snapshot;
  - successful `/integrations` and `/sync/status` responses must contain the documented arrays, including inventory-only mode;
  - every identifier-derived public fingerprint is now keyed HMAC-SHA-256, while raw SHA-256 values remain confined to the exact protected snapshot object;
  - fallback to deprecated `/connection` occurs only for explicit 404/405 endpoint-unavailable responses, never for malformed pages, bounds, network failures, or other HTTP errors.
- Negative fixtures prove unstable membership, malformed integration/schedule/connection shapes, and improper fallback all fail closed. Pagination, legacy shape, GET-only operation, and identifier redaction remain covered.
- Targeted Node test, Prettier, Node syntax, and diff checks pass. Full corrected-head CI/review is restarting.
- Veto MCP remains unavailable; no Veto pass is claimed. No Queue, DLQ, Worker, Nango, D1, alert, or secret mutation was performed.

HOLD remains in force pending immutable-head gates, controlled merge, and protected read-only production reconciliation.
