### #2917 phase checkpoint — inventory review fixes / no production mutation

PR #2961 review found three material evidence-quality issues before merge: the dead-letter target was omitted from normalized consumer topology, the displayed consumer digest included hidden ephemeral IDs, and mutable schedule state was mislabeled as an immutable membership digest.

Commit `739f868bb` fixes all three, adds regression assertions, and separates immutable Nango membership from mutable state digests. Targeted test, formatting, syntax, and diff checks re-pass; all review threads are answered and resolved. The updated head is running the full applicable PR gate set.

Veto MCP remains unavailable and no Veto pass is claimed. No production job ran and no queue, DLQ, Nango, Worker, D1, or alert mutation occurred. Recovery remains **HOLD**.
