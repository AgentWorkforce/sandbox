### #2917 canary-tooling hardening checkpoint / no production mutation

PR #2968 is now at pushed head `2b9a9831c` after a manual failure-window review:

- Worker candidate ownership is recorded before deployment/readback calls that can fail, so an ambiguous settings auto-deploy remains attributable and restorable.
- The unique canary run ID must be absent from the durable D1 admission ledger before any mutation.
- The live-tail URL is normalized to WebSocket form and heartbeat failure closes observability, which is a hard stop.
- The final control HMAC is semantic (queue settings, consumer settings, Worker code/non-gate bindings/dormant gate), so a newly created but equivalent dormant restore version verifies correctly without weakening drift checks.

The focused 7-test suite, Node syntax, Prettier, and diff checks remain green. Full CI/review restarted on this head. No production resource was mutated. Veto MCP remains unavailable; no Veto pass is claimed.
