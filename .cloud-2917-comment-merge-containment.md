### #2917 phase checkpoint — tooling merge deploy trigger contained / no production deploy

PR #2961 merged as `268776540289e6a291337770eb93452d3090fe23`, but the merge commit did not include the requested `[skip ci]` marker. That automatically triggered Deploy run [31277540883](https://github.com/AgentWorkforce/cloud/actions/runs/31277540883).

The lane cancelled the run immediately. Final job readback proves:

- overall run: `CANCELLED`
- full-SST preflight / production job: `CANCELLED` before any steps ran
- production fast path: `SKIPPED`
- production full-SST deploy: `SKIPPED`
- production full-SST retry: `SKIPPED`

The later summary verifier failed because no deploy job completed; that is expected containment evidence, not a deployment failure after mutation. No SST, queue, DLQ, Nango, Worker, D1, or alert mutation was executed by the cancelled run.

Protected read-only inventory run [31277551851](https://github.com/AgentWorkforce/cloud/actions/runs/31277551851) is now running from `main`. Recovery remains **HOLD**.
