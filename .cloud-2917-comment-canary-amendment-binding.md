### Canary V3 amendment-binding checkpoint — no production mutation

PR #2968 is pushed at exact head `0d69973799e3c88069cd25c8a7519c79d5007259`.

The execute authorization now fails before production reads/writes unless the pinned immutable comment:

- belongs specifically to `AgentWorkforce/cloud#2917` (a different issue or PR comment is rejected);
- contains literal `#2917-QUEUE-CANARY-V3`;
- names the exact preflight control HMAC supplied to execute;
- names the exact unique canary run ID supplied to execute; and
- states the fixed admission cap of 10.

The comment body remains SHA-256 pinned and is re-read from GitHub at execution time. This prevents a generic/stale heading-only comment from authorizing a different run.

Focused result: 10/10 tests pass, Node syntax, Prettier, and diff checks pass. Full CI and exact-head review are restarting; no pass is claimed yet. The amendment itself remains unposted and execute remains undispatched.

Veto MCP remains unavailable, so no Veto pass is claimed. No production state was mutated.
