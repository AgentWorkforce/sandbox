### #2917 phase checkpoint — consumer field compatibility fixed / no mutation

Automated review on PR #2964 found one P1: production Consumer detail responses may expose the Worker name as `script`, while the published schema and original fixture used `script_name`.

Fixed and pushed at `f4c88da81`:

- a single normalization helper now accepts `script`, `script_name`, and `scriptName`;
- topology and the public redacted configuration use that same helper;
- the fixture deliberately returns `script` for the main consumer and `script_name` for the DLQ consumer, proving both live and documented shapes resolve to the same Worker;
- targeted tests, Prettier, Node syntax, and diff checks pass.

The review was answered and its thread resolved. Full CI restarted on the new immutable head. No production workflow was dispatched; Queue and Nango holds remain unchanged. Veto MCP remains unavailable and no Veto pass is claimed.
