## Summary

- add a protected, incident-only Queue canary V3 workflow with separate read-only preflight and bounded execution modes
- atomically claim one unique D1 admission row at count 0 / cap 10 before Worker activation, then configure the main consumer to batch 10 / max concurrency 1 while keeping the DLQ paused
- require a live-tail probe, exact preflight control HMAC, one canonical visible and body-SHA-pinned `#2917-QUEUE-CANARY-V3` issue block, RelayAuth health, and matching durable/tail admission count of exactly 10
- extend the read-only production snapshot to prove the active Worker version is latest and publish a keyed code-etag HMAC; require that exact post-deploy pin in preflight, execute, and the immutable amendment before any canary mutation
- require post-dedup processing completion for terminal ACK evidence and stop on any admission-gate hold amplification
- emit one additive Worker telemetry record per admitted message, keyed by the canary run ID, and count only outcomes for that exact admitted-ID set
- persist an encrypted cleanup capsule before mutation; restore through in-process `finally`, EXIT/INT/TERM cleanup, and an independent `always()` cleanup step
- require one joint Worker-ownership + latest-version identity + main/DLQ hold proof to complete within five seconds immediately before every gate-enable or dormant-restore deployment POST; all arms use four-second request timeouts and settle before evidence or errors can return
- require the same fresh joint proof and exact owned active Worker immediately before both enable and dormant settings PATCHes, containing any ambiguous version activation before an explicit deployment step
- reject case/punctuation/spacing-variant duplicate amendment labels, all rendered character-reference syntax, and non-ASCII confusables so visibly conflicting control or Worker pins cannot authorize execution
- upload only AES-256-GCM raw evidence and a redacted public summary; never purge, delete, replay the DLQ, or mutate Nango

## Stop/contain conditions

- admission fails to reach exactly 10 inside 120 seconds or exceeds 10
- live-tail loss, inability to account for all 10 post-processing terminal outcomes, or any gate-held message
- any retry, 5xx, or DLQ movement; unexplained DLQ movement
- RelayAuth health/JWKS drift or D1 growth above 5 MiB
- Queue/consumer/Worker ownership drift, missing positive pause readback, or late Worker version that is not an exact run-owned enabled/dormant clone

## Verification

- `node --test tests/incident-2917-queue-canary-v3.test.mjs tests/incident-2917-snapshot.test.mjs` (22/22)
- `node --check scripts/incident-2917-queue-canary-v3.mjs`
- `npx prettier --check .github/workflows/incident-2917-queue-canary-v3.yml scripts/incident-2917-queue-canary-v3.mjs tests/incident-2917-queue-canary-v3.test.mjs`
- `git diff --check`
- manual mutation allowlist and secrets scan: the only D1 write is the unique retained cap-10 ownership insert; no DELETE/purge/DLQ replay/Nango write path and no credential material

Veto MCP is not exposed in this lane, so no Veto pass is claimed. `veto_diff_review` must be treated as unavailable, not passed.

Closes no issue: production preflight, amendment publication, bounded execution, drain, Nango waves, and closure gates remain separate controlled phases of #2917.

Because the exact admitted-message correlation record is emitted by `webhook-worker`, this reviewed head requires a controlled production Worker deploy and Queue/gate readback before the protected canary preflight.
