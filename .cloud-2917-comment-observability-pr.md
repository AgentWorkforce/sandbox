### #2917 RED/HOLD — stored-observability recovery PR

PR #2969 is open at pushed head `aa24d40ef` to account for the 10 admitted canary events before any retry or drain.

- Queries the exact failed canary window/run marker in stored Workers Logs
- Recovers exact admitted identities in memory, then queries each identity for consume and terminal disposition evidence
- Fails closed on missing or truncated results
- Encrypts every raw log, resource identifier, message identity, and control body; public output contains counts and keyed/HMAC references only
- Requires exact independently restored control and Worker identity pins
- Diagnostic path contains only Queue/Worker GETs plus the documented read-only telemetry query POST; no Queue, Worker, D1, DLQ, or Nango mutation path
- Local gates: 26/26 focused tests, syntax, Prettier, diff, redaction/encryption, and mutation-boundary review green

Veto MCP remains unavailable; no Veto pass is claimed. CI and automated review are pending.

**Production remains independently contained. No diagnostic dispatch, canary retry, drain, replay, DLQ disposition, purge, or Nango action has occurred.**
